<?xml version="1.0"?>
<odoo>
    <record id="view_test_wizard_form" model="ir.ui.view">
        <field name="name">view.test.wizard.form</field>
        <field name="model">test.wizard</field>
        <field name="arch" type="xml">
            <form string="Create new wizard">
                <header>
                    <button name="%(bista_project1.action_create_wizard)d" string="Cancel"
                        type="action" class="oe_highlight" />
                </header>
                <group>
                    <field name="name" />
                    <field name="product" />
                    <field name="employee_id" />

                </group>

                <footer>
                    <button name="action_create_wizard" string="Create" type="object"
                        class="btn-primary" />
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_create_wizard" model="ir.actions.act_window">
        <field name="name">Test Wizard</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">test.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_test_wizard_form" />
        <field name="target">new</field>

    </record>
</odoo>